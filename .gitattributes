# This project's pipeline is executed on Linux/WSL. Force LF endings for
# scripts so a Windows checkout (core.autocrlf=true) doesn't silently break
# bash's `set -o pipefail` and other option parsing.
*.sh text eol=lf
*.py text eol=lf
